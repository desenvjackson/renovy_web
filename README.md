# React + TypeScript + Vite

 ## script para subir para o servidor da aws em prod
permissão da chave: chmod 400 keys-aftermarket-denso.pem
permissão de execução: chmod +x deploy.sh ( macos )
rode: ./deploy.sh ( macos ou windows )